app: spotify-f2p
org: luismeyer
service: spotify-f2p

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: eu-central-1
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "secretsmanager:*"
      Resource: "*"

functions:
  sync:
    handler: ./src/app.handler
    events:
      - schedule: cron(0 */1 * * ? *)
