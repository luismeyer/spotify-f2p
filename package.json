{
  "name": "spotify-f2p",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "build:watch": "npx lerna run build:watch",
    "compile": "tsc --build",
    "compile:watch": "tsc --build --watch",
    "deploy": "npx lerna run deploy",
    "dev": "npx lerna run dev",
    "start": "concurrently 'npm:dev:*' 'npm:*:watch' 'npm run -w @spotify-f2p/serverless ddb:admin'"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "esbuild": "^0.17.19",
    "lerna": "^6.6.2",
    "typescript": "^5.0.4"
  }
}
