{
  "name": "spotify-f2p",
  "version": "1.0.0",
  "private": true,
  "keywords": [],
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "build:watch": "turbo build:watch",
    "compile": "turbo compile",
    "compile:watch": "turbo compile:watch",
    "ddb:dev": "turbo ddb:dev",
    "deploy": "turbo deploy",
    "start": "turbo --concurrency 25 dev build:watch compile:watch"
  },
  "devDependencies": {
    "esbuild": "^0.17.19",
    "turbo": "^1.10.3",
    "typescript": "^5.0.4"
  }
}
